export type PestDoc = { name: string; slug: string };
export type RegionDoc = { name: string; slug: string };
export type SettingsDoc = { phone?: string; defaultOgImage?: string; heroAutoplayDelay?: number };
