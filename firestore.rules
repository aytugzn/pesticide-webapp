rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Browser Firestore is not used. Firebase Authentication does not require
    // Firestore access, while trusted server operations use the Admin SDK.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
